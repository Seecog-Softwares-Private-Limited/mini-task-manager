# Copy this file to properties.env and fill in your values.
# Backend, migrations, seed scripts, and frontend all read from properties.env.

# Database
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=mini_task_manager

# App
JWT_SECRET=replace-with-your-secret

# Optional: frontend API URL (for Next.js)
# NEXT_PUBLIC_API_URL=http://localhost:3000

# Optional: seed overrides
# SEED_USER_PASSWORD=YourStrongPassword123!
# SEED_INVITED_EMAIL=invitee@example.com

# SMTP (optional - see .env.example for full SMTP template)
# SMTP_HOST=localhost
# SMTP_PORT=1025
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@minitaskmanager.local
