export const dynamic = 'force-dynamic';

export default function RecurringTasksLayout({ children }: { children: React.ReactNode }) {
  return <div className="flex min-h-0 flex-1 basis-0 flex-col">{children}</div>;
}
