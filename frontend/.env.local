# Leave NEXT_PUBLIC_API_URL unset: the app uses same-origin /api/v1 (proxied to the API by Next).
# Only set it if the API is on another machine, e.g. NEXT_PUBLIC_API_URL=http://localhost:3007
# Never set it to the Next.js port (e.g. 3008) — login will fail with network errors.
