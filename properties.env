# prod Database
DB_HOST=ls-322b13a438adc6602193777abc66dd7f1b391154.cd0w8y80q80x.ap-south-1.rds.amazonaws.com
DB_PORT=3306
DB_USERNAME=dbmasteruser
DB_PASSWORD=&s0rpr,(G2+1sv7rT3MGKa{c:-j<ebN:
DB_DATABASE=mini_task_manager

# stage Database
# DB_HOST=localhost
# DB_PORT=3306
# DB_USERNAME=root
# DB_PASSWORD=
# DB_DATABASE=mini_task_manager

# App
JWT_SECRET=change-me-in-production

# Ports (defaults: backend 3000, frontend 3001)
PORT=3007
FRONTEND_PORT=3008

# Switch environment: development (local) | production (server)
# On AWS/Lightsail use production so invite emails link to your public app URL.
APP_MODE=production

# Local dev URLs (used when APP_MODE=development)
FRONTEND_URL_LOCAL=http://localhost:3008
CORS_ORIGIN_LOCAL=http://localhost:3008

# Production URLs (used when APP_MODE=production or NODE_ENV=production)
FRONTEND_URL_PRODUCTION=http://3.110.214.243:3000
CORS_ORIGIN_PRODUCTION=http://3.110.214.243:3000

# Optional public API URL (invite emails use FRONTEND_URL_PRODUCTION/invite/:token instead).
PUBLIC_API_URL=http://3.110.214.243:3007

# SMTP — Google Workspace (seecogsoftwares.com). Use App Password from pankaj@seecogsoftwares.com account.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=pankaj@seecogsoftwares.com
SMTP_PASS=xbjrqmkehyuvmjrp
SMTP_FROM="Seecog Softwares <pankaj@seecogsoftwares.com>"

# SMTP — local dev (MailHog). Comment Gmail block above and uncomment below.
# docker compose up -d mailhog  →  inbox at http://localhost:8025
# SMTP_HOST=localhost
# SMTP_PORT=1025
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@minitaskmanager.local

# Razorpay (billing — POST /billing/create-order)
# Keys: https://dashboard.razorpay.com/app/keys
RAZORPAY_KEY_ID=rzp_test_SPsTYCYSeTMunt
RAZORPAY_KEY_SECRET=d7RfW87BpEQMLxUSThw2eNKk
