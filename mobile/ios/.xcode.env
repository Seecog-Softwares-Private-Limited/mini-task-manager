# Used by Xcode build scripts to locate the Flutter SDK.
# Override locally in .xcode.env.local (gitignored) if your Flutter path differs.
export FLUTTER_ROOT="${FLUTTER_ROOT:-$HOME/development/flutter}"
